micronaut:
    application:
        name: vault-config-sample
    config-client:
        enabled: true
    server:
        port: 8080

vault:
    client:
        config:
            enabled: true
        uri: http://172.17.0.2:8200
        token: vaulttoken
        kv-version: V1
        backend: backendv1
        fail-fast: false