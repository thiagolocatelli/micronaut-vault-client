micronaut:
    environments: dev
    application:
        name: vault-config-sample
    config-client:
        enabled: true

vault:
    client:
        config:
            enabled: true
        uri: http://192.168.1.88:8200
        token: vaulttoken
        kv-version: V1
        backend: backendv1
        fail-fast: false